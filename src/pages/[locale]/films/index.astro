---
export const prerender = false;

import Layout from "../../../layouts/Layout.astro";
import EditorialFilmIndex from "../../../components/EditorialFilmIndex.astro";
import { createT } from "../../../i18n";
import {
  isSupportedDetailLocale,
  type SupportedDetailLocale,
} from "../../../lib/localeDetails";

const { locale } = Astro.params;
if (!isSupportedDetailLocale(locale)) return Astro.redirect("/404");
const currentLocale = locale;
const t = createT(currentLocale);
const pageTitles: Record<SupportedDetailLocale, string> = {
  "zh-CN": "电影",
  en: "Films",
  "zh-TW": "電影",
  ja: "映画",
  fr: "Films",
};
---

<Layout
  title={`${pageTitles[currentLocale]} - FemRes`}
  description={t("filmsPage.subtitle")}
  headerVariant="editorial"
  showMusicPlayer={false}
>
  <EditorialFilmIndex locale={currentLocale} basePath={`/${currentLocale}/films`} />
</Layout>
